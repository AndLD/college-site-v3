import { createContext } from 'react'

export const MenuContext = createContext<any>({})
export const ArticlesContext = createContext<any>({})
export const ActionsContext = createContext<any>({})
export const UsersContext = createContext<any>({})
export const NewsContext = createContext<any>({})
