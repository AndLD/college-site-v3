import ProfilePage from '../pages/ProfilePage'

const publicRoutes = [
    {
        path: '/profile',
        component: ProfilePage,
        exact: true
    }
]

export default publicRoutes
